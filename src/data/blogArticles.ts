import { BlogArticle } from '../types';

// Default / fallback articles list (empty — articles are now managed live in Admin Dashboard)
export const blogArticles: Record<string, BlogArticle> = {};

export const blogArticlesList: BlogArticle[] = [];
